<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.dao.mapper.OrderRefundLogMapper">

    <select id="getTotalRefundNum" resultType="int">
        select ifnull(sum(num), 0) from order_refund_log where deleted = false and order_id = #{orderId} and state in (0, 1)
    </select>

</mapper>
