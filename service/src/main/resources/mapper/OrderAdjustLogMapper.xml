<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.OrderAdjustLogMapper">

    <select id="getList" resultType="com.eghm.vo.business.order.adjust.OrderAdjustResponse">
        select
        product_name,
        user_name,
        source_price,
        target_price,
        create_time
        from order_adjust_log where order_no = #{orderNo} and deleted = false order by id desc
    </select>

</mapper>
