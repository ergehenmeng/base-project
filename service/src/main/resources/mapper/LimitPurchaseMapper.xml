<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.LimitPurchaseMapper">

    <select id="getByPage" parameterType="com.eghm.dto.business.purchase.LimitPurchaseQueryRequest" resultType="com.eghm.vo.business.limit.LimitPurchaseResponse">
        select
            id,
            title,
            start_time,
            advance_hour,
            end_time,
            create_time,
            update_time
        from limit_purchase where deleted = 0
        <if test="param.merchantId != null">
            and merchant_id = #{param.merchantId}
        </if>
        <if test="param.queryName != null and param.queryName != ''">
            and title like concat('%', #{param.queryName, typeHandler=com.eghm.handler.mysql.LikeTypeHandler}, '%')
        </if>
        order by id desc
    </select>
</mapper>
