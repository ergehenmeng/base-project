<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.LineMapper">

    <select id="listPage" parameterType="com.eghm.dto.business.line.LineQueryRequest" resultType="com.eghm.vo.business.line.LineResponse">
        select
            t.id,
            t.title,
            r.merchant_name,
            s.title as travelAgencyName,
            t.state,
            t.start_province_id,
            t.start_city_id,
            t.cover_url,
            t.sale_num,
            t.duration,
            t.create_time,
            t.update_time
        from line t
        left join merchant r on t.merchant_id = r.id
        left join travel_agency s on t.travel_agency_id = s.id
        where t.deleted = 0
        <if test="param.state != null">
            and t.state = #{params.state}
        </if>
        <if test="param.duration != null">
            and t.duration = #{params.duration}
        </if>
        <if test = "param.travelAgencyId != null">
            and t.travel_agency_id = #{params.travelAgencyId}
        </if>
        <if test="param.startProvinceId != null">
            and t.start_province_id = #{params.startProvinceId}
        </if>
        <if test="param.startCityId != null">
            and t.start_city_id = #{params.startCityId}
        </if>
        <if test="param.queryName != null and param.queryName != ''">
            and (t.title like  concat('%', #{param.queryName, typeHandler=com.eghm.handler.mysql.LikeTypeHandler}, '%') or
                 r.merchant_name like concat('%', #{param.queryName, typeHandler=com.eghm.handler.mysql.LikeTypeHandler}, '%'))
        </if>
        order by t.id desc
    </select>

    <select id="getByPage" parameterType="com.eghm.dto.business.line.LineQueryDTO" resultType="com.eghm.vo.business.line.LineVO">
        select
        id,
        title,
        start_city_id,
        cover_url,
        total_num,
        duration
        from line where deleted = false and state = 1
        <if test="param.queryName != null and param.queryName != ''">
            and title like concat('%', #{param.queryName, typeHandler=com.eghm.handler.mysql.LikeTypeHandler}, '%')
        </if>
        <if test="param.startProvinceId != null">
            and start_province_id = #{param.startProvinceId}
        </if>
        <if test="param.startCityId != null">
            and start_city_id = #{param.startCityId}
        </if>
        <if test="param.duration != null">
            and duration = #{param.duration}
        </if>
        <if test="param.sortBySale != null">
            <choose>
                <when test="param.sortBySale">
                    order by total_num desc
                </when>
                <otherwise>
                    order by id desc
                </otherwise>
            </choose>
        </if>
    </select>

    <update id="updateScore">
        update line set score = #{score} where id = #{id}
    </update>
</mapper>
