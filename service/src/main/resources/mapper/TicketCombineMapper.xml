<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.TicketCombineMapper">

    <select id="getCombineTicketIds">
        select relation_id from ticket_combine where ticket_id = #{ticketId} and deleted = false
    </select>

</mapper>
