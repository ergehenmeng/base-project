<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.ScoreAccountMapper">

    <update id="updateAccount" parameterType="com.eghm.model.ScoreAccount">
        update score_account set
           pay_freeze = #{payFreeze},
           withdraw_freeze = #{withdrawFreeze},
           version = #{version} + 1,
           amount = #{amount}
        where id = #{id} and version = #{version}
    </update>
</mapper>
