<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.UserCouponMapper">

    <select id="getByPage" resultType="com.eghm.model.vo.coupon.UserCouponResponse" parameterType="com.eghm.model.dto.business.coupon.user.UserCouponQueryRequest">
        select
        t.id,
        t.state,
        t.receive_time as receiveTime,
        t.use_time as useTime,
        r.nick_name as nickName,
        r.mobile
        from user_coupon t
        left join `user` r on t.user_id = r.id
        where t.deleted = false
        <if test="param.couponConfigId != null">
            and t.coupon_config_id = #{param.couponConfigId}
        </if>
        <if test="param.state != null">
            and t.state = #{param.state}
        </if>
        <if test="param.queryName != null and param.queryName != ''">
            and (r.nick_name like concat('%', #{param.queryName} ,'%') or r.mobile = #{param.queryName} )
        </if>
        order by t.id desc
    </select>

    <select id="userCouponPage" resultType="com.eghm.model.vo.coupon.UserCouponVO" parameterType="com.eghm.model.dto.business.coupon.user.UserCouponQueryPageDTO">
        select
        t.id,
        t.state,
        r.title,
        r.coupon_type as couponType,
        r.deduction_value as deductionValue,
        r.discount_value as discountValue,
        r.use_threshold as useThreshold,
        r.use_start_time as useStartTime,
        r.use_end_time as useEndTime,
        r.instruction
        from user_coupon t
        left join coupon_config r on t.coupon_config_id = r.id
        where t.deleted = false and t.user_id = #{param.userId}
        <if test="param.state != null" >
            and t.state = #{param.state}
        </if>
        order by t.id desc
    </select>

    <select id="selectCoupon" resultType="com.eghm.model.vo.coupon.UserCouponBaseVO">
        select
        t.id,
        r.title,
        r.coupon_type as couponType,
        r.deduction_value as deductionValue,
        r.discount_value as discountValue,
        r.use_threshold as useThreshold
        from user_coupon t
        left join coupon_config r on r.id = t.coupon_config_id
        inner join coupon_product s on t.coupon_config_id = s.coupon_config_id
        where
        t.deleted = false and t.state = 0 and t.user_id = #{userId} and s.product_id = #{productId}
        and r.use_start_time &lt;= now() and r.use_end_time &lt;= now()
        order by t.id desc
    </select>

    <select id="countUserReceived" resultType="com.eghm.model.vo.coupon.UserCouponCountVO">
        select
        count(id) as num,
        coupon_config_id as couponId
        from user_coupon where user_id = #{userId} and deleted = false and coupon_config_id in
        <foreach collection="couponIds" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
        group by coupon_config_id
    </select>
</mapper>
