<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.LoginLogMapper">

    <select id="getLastLogin" resultType="com.eghm.model.LoginLog">
        select
        id, member_id, `channel`, ip, create_time, device_brand, device_model, software_version, serial_number
        from login_log where member_id = #{memberId} order by id desc limit 1
    </select>

</mapper>