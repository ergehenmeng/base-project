<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.LineDayConfigMapper">

    <insert id="insertOrUpdate" parameterType="com.eghm.model.LineDayConfig">
        insert into line_day_config(id, line_id, route_index, start_point, end_point, traffic_type, repast, `describe`)
        values (#{id}, #{lineId}, #{routeIndex}, #{startPoint}, #{endPoint}, #{trfficType}, #{repast}, #{describe}) on duplicate key
        update start_point = #{startPoint}, end_point = #{endPoint}, traffic_type = #{trafficType}, repast = #{repast}, `describe` = #{describe}
    </insert>

</mapper>
