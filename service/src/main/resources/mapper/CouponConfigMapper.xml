<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.CouponConfigMapper">

    <update id="updateStock" >
        update coupon_config set stock = stock + #{num}, receive_num = receive_num - #{num}
        where id = #{id} and stock + #{num} &gt;= 0 and deleted = false and state = true
    </update>

    <select id="getByPage" resultType="com.eghm.vo.business.coupon.CouponListVO"
            parameterType="com.eghm.dto.business.coupon.config.CouponQueryDTO">
        select
        id,
        title,
        max_limit as maxLimit,
        coupon_type as couponType,
        deduction_value as deductionValue,
        discount_value as discountValue,
        use_threshold as useThreshold,
        use_start_time as useStartTime,
        use_end_time as useEndTime,
        instruction
        from coupon_config where deleted = false and stock &gt; 0 and state = 1 and `mode` = 1
        and now() between start_time and end_time
    </select>

    <select id="getItemCoupon" resultType="com.eghm.vo.business.coupon.CouponListVO" >
        select
            r.id,
            r.title,
            r.max_limit as maxLimit,
            r.coupon_type as couponType,
            r.deduction_value as deductionValue,
            r.discount_value as discountValue,
            r.use_threshold as useThreshold,
            r.use_start_time as useStartTime,
            r.use_end_time as useEndTime,
            r.instruction
        from coupon_config r left join coupon_item t on t.coupon_config_id = r.id
        where r.deleted = false and r.stock &gt; 0 and r.state = 1 and r.`mode` = 1
        <!-- 商品券或店铺券都过滤 注意 storeId为空时 默认查询自营商品的优惠券 -->
        and ((t.product_id = #{productId} and r.use_scope = 2)  or (r.use_scope = 1 and r.store_id = #{storeId}))
    </select>
</mapper>
