<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.HelpCenterMapper">

    <select id="getList" parameterType="com.eghm.dto.help.HelpQueryRequest" resultType="com.eghm.model.HelpCenter">
        select help_type, ask, answer from help_center where deleted = false and state = true and help_type = #{helpType}
        <if test="queryName != null and queryName != ''">
            and (ask like concat('%', #{queryName, typeHandler=com.eghm.handler.mysql.LikeTypeHandler}, '%') or
                 answer like concat('%', #{queryName, typeHandler=com.eghm.handler.mysql.LikeTypeHandler} ,'%'))
        </if>
        order by sort
    </select>
</mapper>