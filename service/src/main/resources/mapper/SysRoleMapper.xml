<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.SysRoleMapper">

    <select id="getByPage" resultType="com.eghm.vo.sys.SysRoleResponse" >
        select
        id,
        create_time,
        remark,
        role_name,
        role_type,
        update_time
        from sys_role
        where deleted = false
        <if test="merchantId != null">
            and merchant_id = #{merchantId}
        </if>
        <if test="merchantId == null">
            and merchant_id is null
        </if>
        <if test="queryName != null and queryName != ''">
            and role_name like concat('%', #{queryName, typeHandler=LikeTypeHandler},'%')
        </if>
        order by id desc
    </select>

    <select id="getRoleMenu" resultType="string">
        select t.menu_id
        from sys_role_menu t, sys_menu r
        where t.menu_id = r.id and role_id = #{roleId} and r.grade = 2
    </select>

    <delete id="deleteRoleMenu">
        delete
        from sys_role_menu
        where role_id = #{roleId}
    </delete>

    <insert id="batchInsertRoleMenu">
        insert into sys_role_menu (role_id, menu_id) values
        <foreach collection="menuIdList" item="item" separator=",">
            (#{roleId}, #{item})
        </foreach>
    </insert>

    <select id="countByRoleType" resultType="int">
        select count(t.id)
        from sys_role t
        left join sys_user_role r on t.id = r.role_id
        where r.user_id = #{userId}
          and t.deleted = false and t.role_type = #{roleType}
    </select>
</mapper>