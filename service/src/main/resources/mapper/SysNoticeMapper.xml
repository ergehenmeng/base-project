<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.SysNoticeMapper">

    <select id="getByPage" resultType="com.eghm.vo.notice.NoticeResponse" parameterType="com.eghm.dto.notice.NoticeQueryRequest">
        select id, title, notice_type, state, create_time, update_time
        from sys_notice
        where deleted = false
          <if test="param.queryName != null and param.queryName != ''">
            and title like concat('%', #{param.queryName, typeHandler=com.eghm.handler.mysql.LikeTypeHandler}, '%')
          </if>
        <if test="param.noticeType != null">
            and notice_type = #{param.noticeType}
        </if>
        <if test="param.state != null">
            and state = #{param.state}
        </if>
        order by update_time desc, id desc
    </select>

    <select id="getTopList" resultType="com.eghm.model.SysNotice">
        select id, title
        from sys_notice
        where state = 1
          and deleted = false
        order by update_time desc limit #{noticeLimit}
    </select>

</mapper>