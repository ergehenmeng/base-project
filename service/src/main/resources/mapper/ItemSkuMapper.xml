<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.ItemSkuMapper">
    
    <update id="updateStock" >
        update item_sku set stock = stock + #{num}, sale_num = sale_num - #{num}
        where id = #{skuId} and deleted = false and stock + #{num} &gt;= 0
    </update>

    <update id="addStock" >
        update item_sku set stock = stock + #{num}
        where id = #{skuId} and deleted = false and stock + #{num} &gt;= 0 and item_id = #{itemId}
    </update>

    <select id="calcTotalNum" resultType="integer">
        select sum(sale_num + virtual_num) from item_sku where item_id = #{itemId} and deleted = false
    </select>
</mapper>
