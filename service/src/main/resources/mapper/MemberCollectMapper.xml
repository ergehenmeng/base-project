<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.mapper.MemberCollectMapper">

    <select id="getByPage" resultType="com.eghm.vo.business.collect.MemberCollectVO">
        select
        collect_id,
        collect_type
        from member_collect where member_id = #{memberId} and deleted = false and state = 1
        order by id desc
    </select>

</mapper>
