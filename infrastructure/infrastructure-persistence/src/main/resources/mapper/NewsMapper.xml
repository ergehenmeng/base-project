<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eghm.infrastructure.persistence.mybatis.mapper.NewsMapper">

    <select id="listPage" resultType="com.eghm.application.shared.vo.business.news.NewsResponse" parameterType="com.eghm.application.shared.dto.business.news.NewsQueryRequest">
        select
            id,
            title,
            depict,
            image,
            sort,
            state,
            tag_name,
            comment_support,
            praise_num,
            create_time,
            update_time
        from news where deleted = false and code = #{param.code}
        <if test="param.queryName != null and param.queryName != ''">
            and title like concat('%', #{param.queryName, typeHandler=LikeTypeHandler}, '%')
        </if>
        order by sort, id desc
    </select>

    <select id="getByPage" resultType="com.eghm.application.shared.vo.business.news.NewsVO">
        select  id, title, depict, tag_name, image, praise_num, update_time
        from news where deleted = false
        <if test="queryName != null and queryName != ''">
            and title like concat('%', #{queryName, typeHandler=LikeTypeHandler}, '%')
        </if>
        order by sort, id desc
    </select>

    <update id="updatePraiseNum">
        update news set praise_num = praise_num + #{praiseNum} where id = #{id}
    </update>

    <select id="getList" resultType="com.eghm.application.shared.vo.business.news.NewsVO">
        select
            id,
            title,
            depict,
            tag_name,
            image,
            state,
            deleted
        from news where id in
        <foreach collection="newsIds" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </select>

</mapper>   
