# base-project-3.2.x 代码重构计划

## 项目概述
base-project-3.2.x 是一个基于Spring Boot 3.2.12的多模块企业级管理系统，包含webapp-server（移动端）、manage-server（管理后台）和service（业务逻辑与数据访问层）三个主要模块。

## 重构目标
针对该项目实施代码重构，重点关注：
1. 代码结构优化
2. 重复代码消除
3. 配置优化
4. 安全增强

## 一、代码结构优化

### 1.1 包结构规范化
- **现状问题**：当前包结构按功能划分（如controller、service、dto等），但缺乏明确的业务领域边界
- **重构方案**：
  ```
  com.eghm
  ├── domain                    # 领域模型层
  │   ├── user                  # 用户领域
  │   │   ├── entity            # 实体类
  │   │   ├── valueobject       # 值对象
  │   │   ├── repository        # 仓储接口
  │   │   └── service           # 领域服务
  │   └── order                 # 订单领域（示例）
  ├── application             # 应用层
  │   ├── dto                 # 数据传输对象
  │   ├── vo                  # 视图对象
  │   ├── event               # 事件定义
  │   └── service             # 应用服务
  ├── infrastructure          # 基础设施层
  │   ├── persistence         # 持久化实现
  │   ├── cache               # 缓存实现
  │   ├── mq                  # 消息队列实现
  │   └── config              # 配置类
  └── interfaces              # 接口层
      ├── web                 # Web接口
      ├── websocket           # WebSocket接口
      └── rest                # REST接口
  ```

### 1.2 模块职责清晰化
- **service模块**：纯业务逻辑层，移除所有Web相关的配置和组件
- **webapp-server**：移动端API接口，专注于移动端业务场景
- **manage-server**：管理后台API接口，专注于管理功能

### 1.3 分层架构完善
- 引入领域驱动设计（DDD）思想
- 明确各层职责边界，减少跨层调用
- 增加防腐层（Anti-Corruption Layer）处理外部系统集成

## 二、重复代码消除

### 2.1 通用工具类优化
- **现状**：存在多个相似的工具类，如StringUtil、DataUtil等
- **重构方案**：
  - 整合Hutool和自定义工具类，减少重复实现
  - 提取公共的验证逻辑到统一的ValidationUtil
  - 统一日期时间处理，避免LocalDateTime与Date混用

### 2.2 控制器层代码优化
- **现状**：大量CRUD操作在不同控制器中重复实现
- **重构方案**：
  - 创建基础控制器BaseController，封装通用CRUD方法
  - 实现泛型基类，支持不同类型实体的通用操作
  - 统一响应格式和异常处理

### 2.3 服务层代码优化
- **现状**：类似的数据验证、缓存操作在多个服务中重复
- **重构方案**：
  - 创建通用的Service基类BaseService
  - 抽象通用的缓存操作模板
  - 实现统一的事务管理策略

### 2.4 配置类优化
- **现状**：配置类分散，部分配置在多个地方重复定义
- **重构方案**：
  - 统一配置属性定义到专门的配置类中
  - 使用@Validated注解进行配置验证
  - 提供配置项的默认值和文档说明

## 三、配置优化

### 3.1 外部化配置改进
- **现状**：配置散落在多个yml文件中，缺少统一管理
- **重构方案**：
  - 按环境分离配置（dev、test、prod）
  - 使用配置中心（如Nacos、Apollo）进行集中管理
  - 实现配置热更新机制

### 3.2 数据库连接优化
- **现状**：HikariCP配置可能未优化
- **重构方案**：
  - 调整连接池参数（minimum-idle、maximum-pool-size等）
  - 启用连接池监控和统计
  - 实现读写分离和分库分表策略

### 3.3 缓存配置优化
- **现状**：Redisson和本地缓存配置可能存在性能瓶颈
- **重构方案**：
  - 优化Redis连接池配置
  - 实现缓存预热和缓存穿透防护
  - 使用缓存注解简化缓存操作

### 3.4 安全配置优化
- **现状**：安全性配置可能不足
- **重构方案**：
  - 集成Spring Security进行身份认证和授权
  - 实现JWT Token的安全管理
  - 添加CSRF、XSS等安全防护

## 四、安全增强

### 4.1 身份认证强化
- **现状**：现有的token机制可能安全性不足
- **重构方案**：
  - 升级JWT实现，使用更强的加密算法
  - 实现Token刷新机制
  - 添加Token黑名单管理

### 4.2 输入验证强化
- **现状**：可能存在输入验证不足的问题
- **重构方案**：
  - 统一使用Hibernate Validator进行参数验证
  - 实现自定义验证注解
  - 添加全局异常处理器

### 4.3 SQL注入防护
- **现状**：虽然使用MyBatis-Plus，但仍需检查动态SQL安全性
- **重构方案**：
  - 确保所有动态SQL使用预编译语句
  - 实施SQL审计和监控
  - 使用MyBatis-Plus的SQL注入防护机制

### 4.4 敏感信息处理
- **现状**：敏感信息可能在日志中泄露
- **重构方案**：
  - 实现敏感信息脱敏注解和序列化器
  - 在日志记录中自动脱敏
  - 对密码等敏感信息进行强加密存储

## 五、实施步骤

### 第一阶段：结构优化（2-3周）
1. 设计新的包结构和分层架构
2. 迁移现有代码到新结构
3. 创建基础抽象类和通用组件

### 第二阶段：代码优化（3-4周）
1. 消除重复代码
2. 优化工具类和配置类
3. 实现通用的CRUD操作

### 第三阶段：配置优化（1-2周）
1. 优化数据库连接配置
2. 优化缓存配置
3. 集成配置中心

### 第四阶段：安全增强（2-3周）
1. 实现安全认证机制
2. 添加输入验证和防护措施
3. 实现敏感信息处理

### 第五阶段：测试和部署（1-2周）
1. 进行全面的功能测试
2. 性能测试和安全测试
3. 生产环境部署

## 六、风险控制

1. **向后兼容性**：确保重构不影响现有API接口
2. **数据一致性**：在重构过程中保证数据完整性
3. **性能影响**：评估重构对系统性能的影响
4. **团队协作**：建立代码审查机制，确保重构质量

## 七、预期收益

1. **代码质量提升**：减少重复代码，提高可维护性
2. **性能优化**：通过配置优化提升系统性能
3. **安全性增强**：提升系统安全防护能力
4. **扩展性改善**：为未来功能扩展提供更好的架构支持